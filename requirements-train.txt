# Training dependencies. Install a PyTorch build compatible with your CUDA driver
# before, or let pip select the appropriate build for a CPU-only environment.
-r requirements-eval.txt
pandas>=2.0,<3
PyYAML>=6.0,<7
torch>=2.2
transformers>=4.45
datasets>=2.18
accelerate>=0.30
peft>=0.12
trl>=0.10
